import React from 'react';

export const CarouselCard1 = ({cardInfo}) => (
		<span>
        	{cardInfo}...Cards1...
        </span>
);

export const CarouselCard2 = ({cardInfo}) => (
		<span>
        	{cardInfo}...Cards2...
        </span>
);

export const CarouselCard3 = ({cardInfo}) => (
		<span>
        	{cardInfo}...Cards3...
        </span>
);